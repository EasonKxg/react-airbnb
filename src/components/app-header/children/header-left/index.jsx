import React, { memo } from "react";
import { LeftStyleWrapper } from "./style";
const HeaderLeft = memo(() => {
  return <LeftStyleWrapper>HeaderLeft</LeftStyleWrapper>;
});

export default HeaderLeft;
