import React, { memo } from "react";
import { DetailInfoStyleWrapper } from "./style";

const DetailInfo = memo(() => {
  return <DetailInfoStyleWrapper>DetailInfo</DetailInfoStyleWrapper>;
});

export default DetailInfo;
